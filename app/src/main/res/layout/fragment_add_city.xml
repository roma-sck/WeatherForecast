<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:background="@color/main_background_color"
        tools:context=".ui.AddCityFragment">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/enter_city_name_text"/>

        <EditText
            android:id="@+id/entered_city_name"
            android:layout_marginTop="@dimen/entered_city_name_margin_top"
            android:hint="@string/enter_city_name_hint"
            android:textColorHint="@color/text_hint_color"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <Button
            android:id="@+id/button_add_city"
            android:layout_marginTop="@dimen/btn_save_city_name_margin_top"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_save_city_name_text"
            android:onClick="onFragmentViewClick" />

    </RelativeLayout>
</layout>